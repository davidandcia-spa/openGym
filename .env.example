# Passkeys / app origin
RP_ID=localhost
ORIGIN=http://localhost:8080
WEB_PORT=8080
RP_NAME=openGym

# Optional administration
ADMIN_UIDS=
INVITE_ONLY=false
SESSION_DAYS=90

# AI Coach — keep the real key only in .env, never commit it.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
